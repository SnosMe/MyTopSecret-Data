{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${command:cmake.launchTargetPath}",
      "args": [
        "-e",
        "data.mtsd",
        "-o",
        "data.mtsd.bin"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}